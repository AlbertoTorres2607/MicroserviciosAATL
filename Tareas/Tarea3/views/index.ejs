<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Mi Agenda</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/style.css">
</head>
<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">📞 Mi Agenda</a>
        </div>
    </nav>

    <div class="container mt-4">
        <div class="d-flex justify-content-between align-items-center mb-4">
            <h1>Lista de Contactos</h1>
            <a href="/contactos/new" class="btn btn-success">+ Nuevo Contacto</a>
        </div>

        <!-- Búsqueda -->
        <form method="GET" class="mb-4">
            <div class="input-group">
                <input type="text" class="form-control" name="nombres" 
                       placeholder="Buscar por nombres..." value="<%= typeof query !== 'undefined' ? query : '' %>">
                <button class="btn btn-outline-secondary" type="submit">Buscar</button>
                <a href="/" class="btn btn-outline-secondary">Limpiar</a>
            </div>
        </form>

        <% if (contactos.length === 0) { %>
            <div class="alert alert-info">
                No hay contactos registrados. <a href="/contactos/new">¡Crea el primero!</a>
            </div>
        <% } else { %>
            <div class="row">
                <% contactos.forEach(contacto => { %>
                    <div class="col-md-6 col-lg-4 mb-3">
                        <div class="card">
                            <div class="card-body">
                                <h5 class="card-title"><%= contacto.nombres %> <%= contacto.apellidos %></h5>
                                <p class="card-text">
                                    <strong>📧</strong> <%= contacto.correo %><br>
                                    <strong>📱</strong> <%= contacto.celular %>
                                </p>
                                <div class="btn-group" role="group">
                                    <a href="/contactos/<%= contacto._id %>" class="btn btn-info btn-sm">Ver</a>
                                    <a href="/contactos/<%= contacto._id %>/edit" class="btn btn-warning btn-sm">Editar</a>
                                    <form method="POST" action="/contactos/<%= contacto._id %>?_method=DELETE" 
                                          style="display: inline;" 
                                          onsubmit="return confirm('¿Estás seguro de eliminar este contacto?')">
                                        <button type="submit" class="btn btn-danger btn-sm">Eliminar</button>
                                    </form>
                                </div>
                            </div>
                        </div>
                    </div>
                <% }) %>
            </div>
        <% } %>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>